<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only needs to give the window sane defaults. -->
    <style name="Theme.OnlineWallpaper" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
