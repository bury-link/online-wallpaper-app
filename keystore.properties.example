# Copy to keystore.properties (ignored by Git), then fill with real values.
# Keep the keystore and all four values in a secure backup. Losing the upload key complicates future updates.
storeFile=release/online-wallpaper-upload.jks
storePassword=CHANGE_ME
keyAlias=online-wallpaper-upload
keyPassword=CHANGE_ME
